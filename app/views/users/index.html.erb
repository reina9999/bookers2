<main>
<%= render 'books/book', book: @user,user: current_user %>
<p id="notice"></p>
<div class="container">
  <div class ="row">
    <div class="col-md-3">
 <div class="col-md-8 offset-md-1">
    <h2>Users</h2>
      <p>
      <table>
    <thead>
    <tr>
    <th>Image</th>
    <th>name</th>
    <th colspan="3"></th>
    </tr>
    </thead>
   <tr>
    <% @users.each do |user| %>
    <tr>
    <td><%= image_tag user.get_profile_image(100,100) %></td>
    <td><%= user.name %></td>
    <td><%= link_to"Show",user_path(user) %></td>
    </tr>
　　<% end %>
　　 </tbody>
     </thead>
     </table>
    </div>
    </div>
    </div>
  </div>
</main>